# test-seite-beta

Inhalt dieses Ordners = Repo-Root für Vercel.

- `vercel.json` muss im Repo-Root liegen (Vercel liest sie nur dort). Sie aktiviert `cleanUrls`
  (`/beyond-marketing/ansatz` statt `/beyond-marketing/ansatz.html`) und leitet `/` auf `/beyond-marketing` um.
- `beyond-marketing/` = die komplette App (Seiten, Blog, `assets/`, `js/`).

Live: https://test-seite-beta.vercel.app/beyond-marketing
